public class day_28_01_2025 {
    
}
// import java.util.HashSet;

// class Solution {
//     public boolean containsDuplicate(int[] nums) {
//         HashSet<Integer> set = new HashSet<>(); // Create a new HashSet to store unique integers.
//         for (int num : nums) { // Iterate through each number in the array.
//             if (!set.add(num)) { // Try to add the number to the HashSet.
//                 return true; // If the number already exists, set.add() returns false, meaning a duplicate is found.
//             }
//         }
//         return false; // If the loop completes, no duplicates were found, so return false.
//     }
// }